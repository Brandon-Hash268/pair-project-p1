<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Buy Stock</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f4f6f9; /* Light background for better contrast */
            padding-top: 80px; /* Space for fixed navbar */
            font-family: 'Open Sans', sans-serif;
        }
        .form-container {
            max-width: 600px;
            margin: auto;
            background-color: #ffffff; /* White background for contrast */
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.1); /* Softer shadow for depth */
        }
        h2 {
            margin-bottom: 20px;
            font-size: 28px;
            font-weight: 600;
            text-align: center;
            color: #00237A; /* Deep blue for accent */
        }
        .img-preview {
            max-width: 100px;
            height: auto;
            margin-bottom: 15px;
            display: block;
            border-radius: 8px; /* Rounded corners for the image */
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1); /* Slight shadow for image */
        }
        .stock-details {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }
        .stock-info {
            font-size: 16px;
        }
        .stock-info h5 {
            margin: 0;
            font-size: 18px;
            font-weight: 600;
            color: #444444; /* Slightly darker grey */
        }
        .stock-info p {
            margin: 0;
            font-size: 14px;
            color: #777777; /* Lighter grey for stock code */
        }
        .form-group {
            margin-bottom: 20px;
        }
        input[type="number"] {
            border-radius: 8px;
            border: 1px solid #dddddd;
            padding: 10px;
            width: 100%;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.05); /* Slight shadow for input */
        }
        button {
            width: 100%;
            padding: 12px;
            background-color: #00237A;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        button:hover {
            background-color: #001b5e;
        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>
<div class="form-container">
    <h2>Sell Stock</h2>
    <div class="stock-details">
        <div class="stock-info">
            <h5><%= stock.companyName %></h5>
            <p>Code: <%= stock.code %></p>
            <p>Owned: <%= user.Portofolios && user.Portofolios.length > 0 ? user.Portofolios[0].totalStock : 0 %></p>
            <p>Price: <%= currency(stock.price)%></p>
        </div>
        <img class="img-preview" src="<%= stock.imgUrl %>" alt="Stock Image">
    </div>
    <form action="/stock/<%= stock.id %>/sell" method="post">
        <div class="form-group">
            <label for="totalStock">Quantity to Buy:</label>
            <input type="number" id="totalStock" name="totalStock" min="1" placeholder="Enter quantity" required>
        </div>
        <button type="submit">Sell Stock</button>
        <% if (error) { %>
            <% error.forEach(e => { %>
                <p style="color: red;"><%= e %></p>
            <% }) %>    
        <% } %>
    </form>
</div>

</body>
</html>
